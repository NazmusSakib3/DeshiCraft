# Base URL of the DeshiCraft API. In dev, Vite proxies /api to localhost:5000,
# so this can be left as /api. In production set it to your deployed API origin,
# e.g. https://deshicraft-api.onrender.com/api
VITE_API_URL=/api
